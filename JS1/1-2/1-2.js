/**
 * Write a program that takes a string as input and displays the length of the string
 * @param {string} string
 * @returns length of string input
 */
const takeLength = (string) => {
  return string.length;
};
module.exports = takeLength;
