/**
 *Write a program that takes a number as input and displays its square.
 * @param {number} number
 * @returns quare of input number
 */
const takeSquare = (number) => {
  return Math.pow(number, 2);
};
module.exports = takeSquare;
